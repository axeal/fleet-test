apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-lm-app-kube-state-metrics
  namespace: monitoring
spec:
  template:
    spec:
      containers:
        - name: kube-state-metrics
          image: registry.k8s.io/kube-state-metrics/kube-state-metrics:v2.8.0
