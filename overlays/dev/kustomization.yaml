resources:
- ../../base

commonAnnotations:
  aristeo.build.environment: eepp

patches:
  - target:
      group: ''
      version: v1
      kind: Deployment
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        labels:
          app: nginx
          tier: example
        name: nginx
  - target:
      group: ''
      version: v1
      kind: Deployment
    patch: |
      - op: add 
        path: /spec/template/spec/containers/0/env/-
        value:
          name: ENVIRONMENT
          value: EEPP
