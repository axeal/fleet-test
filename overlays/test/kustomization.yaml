resources:
- ../../base

commonAnnotations:
  aristeo.build.environment: eepp

patches:
  - target:
      group: ''
      version: v1
      kind: Deployment
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        labels:
          app: nginx
          tier: test
        name: nginx

patchesJson6902:
  - target:
      group: ''
      version: v1
      kind: Deployment
      name: nginx
    patch: |
      - op: add 
        path: /spec/template/spec/containers/0/env/-
        value:
          name: ENVIRONMENT
          value: TEST